++ Running the App on Device
  -- make sure device is connected with command 'adb devices' (for android debuggin)

  -- After the device is connect, run on app on the device with the 'ionic cordova run android' command.